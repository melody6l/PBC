# 方案：多文件超链接分行显示

## 问题

当清单中 1 条明细匹配到多个资料文件时：

- **Excel 导出**：openpyxl 限制每个单元格只能设置 1 个超链接，当前代码只取了 `matched_files[0]`，其余文件丢失超链接
- **页面表格**：已正确分行显示（每个 `<div>` 一个链接），无需改动

## 当前代码问题定位

`excel_handler.py` 第 117-125 行：

```python
# 当前：文本写入了所有文件名，但超链接只绑了第一个
matched_str = "\n".join(result["matched_names"])
ws.cell(row=row_idx, column=link_col, value=matched_str)
if result["matched_files"]:
    link_path = result["matched_files"][0]  # ← 只有第一个有超链接
    ws.cell(row=row_idx, column=link_col).hyperlink = link_url
```

## 解决方案：拆行 + 合并原始列

### 核心思路

| 条件 | 行数 | 处理 |
|------|------|------|
| 匹配 0 个文件 | 1 行 | 原始数据正常写，状态=未获取，链接列空 |
| 匹配 1 个文件 | 1 行 | 和当前一样，无变化 |
| 匹配 N 个文件 | N 行 | 原始数据列 + 状态列 **纵向合并**，文件超链接列**每行一个** |

### 效果示意

清单明细 A 匹配了 3 个文件，明细 B 没匹配到：

```
┌──────────┬──────────┬──────────┬──────────┬─────────────────────────┐
│ 序号     │ 资料名称 │ 备注     │ 核对结果 │ 文件超链接              │
├──────────┼──────────┼──────────┼──────────┼─────────────────────────┤
│          │          │          │          │ 📎 文件A1.xlsx  →       │ ← 可点击
│          │ 明细A    │ 重要     │ 已获取   ├─────────────────────────┤
│          │          │          │          │ 📎 文件A2.pdf   →       │ ← 可点击
├──────────┤          │          │          ├─────────────────────────┤
│ (合并)   │          │          │          │ 📎 文件A3.docx  →       │ ← 可点击
├──────────┼──────────┼──────────┼──────────┼─────────────────────────┤
│ 2        │ 明细B    │          │ 未获取   │                         │
├──────────┼──────────┼──────────┼──────────┼─────────────────────────┤
│          │          │          │          │ 📎 文件C1.xlsx  →       │
│ (合并)   │ 明细C    │ 普通     │ 已获取   ├─────────────────────────┤
│          │          │          │          │ 📎 文件C2.pdf   →       │
└──────────┴──────────┴──────────┴──────────┴─────────────────────────┘
```

- 原始数据列（序号、名称、备注…）+ 核对结果列：**纵向合并为一个单元格**，垂直居中
- 文件超链接列：**每个文件独占一行**，单独设置超链接，均可点击

## 代码改动

### excel_handler.py `export_results()` 函数

替换第 89-127 行的写入逻辑：

```python
from openpyxl.styles import Border, Side

# 在样式定义区新增
thin_border = Border(
    left=Side(style='thin'),
    right=Side(style='thin'),
    top=Side(style='thin'),
    bottom=Side(style='thin'),
)

# 写入数据
row_idx = 2
for result in results:
    # 找到对应的原始数据行
    orig_row = None
    for row in data:
        val = row[name_col_index] if len(row) > name_col_index else ""
        if val and val.strip() == result["checklist_name"]:
            orig_row = row
            break

    matched_files = result.get("matched_files", [])
    matched_names = result.get("matched_names", [])
    n_files = len(matched_files)
    n_rows = max(1, n_files)  # 至少1行

    for sub in range(n_rows):
        # ---- 原始数据列 + 状态列：仅第一行写入 ----
        if sub == 0:
            if orig_row:
                for col_num, col_idx in enumerate(valid_cols, 1):
                    cell_val = orig_row[col_idx] if len(orig_row) > col_idx else ""
                    ws.cell(row=row_idx, column=col_num, value=cell_val)

            # 核对结果
            status_col = len(valid_cols) + 1
            status_cell = ws.cell(row=row_idx, column=status_col, value=result["status"])
            status_cell.alignment = Alignment(horizontal="center", vertical="center")
            if result["status"] == "已获取":
                status_cell.fill = green_fill
            elif result["status"] == "部分获取":
                partial_fill = PatternFill(start_color="FFEB9C", end_color="FFEB9C", fill_type="solid")
                status_cell.fill = partial_fill
            else:
                status_cell.fill = red_fill

        # ---- 文件超链接列：每行一个文件 ----
        link_col = len(valid_cols) + 2
        if sub < n_files:
            file_name = matched_names[sub] if sub < len(matched_names) else ""
            file_path = matched_files[sub]
            link_url = "file:///" + file_path.replace("\\", "/")

            cell = ws.cell(row=row_idx, column=link_col, value=file_name)
            cell.hyperlink = link_url
            cell.font = Font(color="0563C1", underline="single")
            cell.alignment = Alignment(vertical="center")

        row_idx += 1

    # ---- 多行时合并原始列和状态列 ----
    if n_rows > 1:
        merge_start = row_idx - n_rows
        merge_end = row_idx - 1
        for col_num in range(1, len(valid_cols) + 1):
            ws.merge_cells(
                start_row=merge_start, start_column=col_num,
                end_row=merge_end, end_column=col_num,
            )
            # 合并后设置垂直居中和边框
            ws.cell(row=merge_start, column=col_num).alignment = Alignment(
                horizontal="center", vertical="center", wrap_text=True
            )
        # 合并状态列
        status_col = len(valid_cols) + 1
        ws.merge_cells(
            start_row=merge_start, start_column=status_col,
            end_row=merge_end, end_column=status_col,
        )
```

### 无需改动的部分

| 模块 | 说明 |
|------|------|
| `static/js/main.js` | 页面表格已正确分行显示多个链接 |
| `app.py` | 后端数据结构 `matched_files` 已是列表，无需修改 |
| `matcher.py` | 匹配引擎已支持返回多个匹配文件 |

## 改动文件清单

| 文件 | 改动量 | 说明 |
|------|-------|------|
| `excel_handler.py` | ~40 行 | 重写超链接写入逻辑：拆行 + 合并单元格 |

## 验证步骤

1. 准备一个清单，其中至少 1 条明细能匹配多个文件
2. 执行匹配 → 导出 Excel
3. 打开导出的 Excel 验证：
   - 多文件的明细项占多行，每个文件一行
   - 原始数据列和状态列已合并，垂直居中
   - 每个文件超链接均可点击打开
   - 单文件和无匹配的明细项显示正常
   - 颜色标识（绿/黄/红）正常显示
